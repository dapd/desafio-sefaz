CREATE TABLE USUARIO (
COD_USUARIO INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
NOME VARCHAR (100) NOT NULL,
EMAIL VARCHAR (50) NOT NULL UNIQUE,
SENHA VARCHAR (32) NOT NULL
);
